import React from 'react';

function TextInput (content) {
  const Return_input =()=>{
    
  } 
  return (
    <div>
      <input type="button" value={content} onClick={}>
      </input>
    </div>
  );
};

export default TextInput;